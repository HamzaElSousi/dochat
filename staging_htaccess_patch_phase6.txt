# Phase 6: Lead Capture routes
# Append these routing rules to the existing .htaccess CGI section,
# after the Phase 5 widget.js rule and before the PHP section.
# Pattern matches the /app.cgi/ convention used by all Phase 4/5 rules.

# Lead capture submission endpoint (widget → server)
RewriteRule ^dochat/api/leads/?$           /app.cgi/dochat/api/leads [QSA,L]

# Public settings endpoint (widget fetches book_call_url on init)
RewriteRule ^dochat/api/settings/?$        /app.cgi/dochat/api/settings [QSA,L]

# Admin settings page (GET) and save (POST)
RewriteRule ^dochat/admin/settings/?$      /app.cgi/dochat/admin/settings [QSA,L]
